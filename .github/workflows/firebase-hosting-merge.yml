# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - name: Create environment file
        env:
          REALM_API_KEY: ${{ secrets.REALM_API_KEY }}
          REALM_APP_ID: ${{ secrets.REALM_APP_ID }}
          PASSWORD: ${{ vars.PASSWORD }}
        run: |
          mkdir -p src/environment
          touch src/environment/environment.ts
          echo "export const environmentValues = {
            REALM_API_KEY: '$REALM_API_KEY',
            REALM_APP_ID: '$REALM_APP_ID',
            PASSWORD: '$PASSWORD',
          };" > src/environment/environment.ts && echo "File created successfully" || echo "Error creating file"
          cat src/environment/environment.ts
          ls src/environment/
      - run: ls
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ROOMFOODAPP_JDPS }}
          channelId: live
          projectId: roomfoodapp-jdps
